@model IEnumerable<BookStore.Models.order_item>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.book.book_title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.hub_travel_cost.cost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.hubs_admins.admin_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.order.order_state)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.no_of_items)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cost)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.book.book_title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.hub_travel_cost.cost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.hubs_admins.admin_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.order.order_state)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.no_of_items)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cost)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
